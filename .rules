# REMSOL — Zed AI Assistant Rules

## Project Overview

REMSOL (Rust-based Electromagnetic Multi-layer Solver) computes **effective refractive
indices** and **field profiles** of guided photonic modes in 1D multilayer (planar slab)
structures. The core solver is written in Rust for performance and exposed to Python via
PyO3 bindings, built with Maturin.

---

## Tech Stack

| Layer           | Tool / Version                                       |
|-----------------|------------------------------------------------------|
| Core solver     | Rust, edition 2021                                   |
| Python bindings | PyO3 v0.24                                           |
| Build bridge    | Maturin >=1.7                                        |
| Python env      | uv                                                   |
| Testing         | cargo test (Rust), pytest (Python), pytest-benchmark |
| Versioning      | python-semantic-release (bumps Cargo.toml version)   |
| CI              | GitHub Actions                                       |
| Docs            | Jupyter Book                                         |
| Logging bridge  | pyo3-log (routes Rust `log` macros to Python `logging`) |

---

## Repository Layout

```
src/
  lib.rs                — PyO3 module root; registers all Python-visible types; calls pyo3_log::init()
  enums.rs              — Polarization (TE/TM), BackEnd (Transfer/Scattering), Normalization (MaxField/Power) enums
  layer.rs              — Layer struct (n: Complex<f64>, d: f64) + LayerCoefficientVector
  multilayer.rs         — MultiLayer: solver, field calculation, Python API
  transfer_matrix.rs    — Transfer Matrix Method (TMM) backend + kz_physical helper
  scattering_matrix.rs  — Scattering Matrix Method (SMM) backend
  bin/remsol.rs         — CLI binary entry point (uses SerdeLayer shim for TOML I/O)

test/
  test_slab.py          — pytest: single slab, coupled slab, asymmetric coupled slab
  test_field.py         — pytest: MaxField/Power normalization, field_complex on lossy slab
  test_pec.py           — pytest: PEC boundary condition tests

remsol.pyi              — Python type stubs (must stay in sync with lib.rs pymodule)
pyproject.toml          — Maturin + uv configuration
Cargo.toml              — Rust crate metadata and dependencies
CHANGELOG.md            — Auto-generated by python-semantic-release
docs/                   — Jupyter Book documentation + example notebooks
.github/workflows/      — CI: test.yml, publish_pypi.yml, publish_crate.yml, pages.yml
```

---

## Critical Build Rule

**After ANY change to Rust source files, the Python extension must be rebuilt before
running Python tests or using the Python API:**

```bash
uv run maturin develop --uv
uv sync --reinstall-package remsol
```

Both commands are required. `maturin develop --uv` compiles the Rust code and installs
the new wheel, but `uv` may restore a cached version of the old package on the next
`uv run`. Running `uv sync --reinstall-package remsol` forces `uv` to pick up the
freshly built extension from the local source tree.

Forgetting one or both steps is the most common source of confusion — Python will
silently use the old compiled extension.

---

## All Commands

```bash
# Rebuild Python extension (required after any .rs change)
uv run maturin develop --uv
uv sync --reinstall-package remsol

# Run Rust unit tests (no Python needed)
cargo test --lib

# Run all Python integration tests
uv run pytest test/

# Run only benchmarks
uv run pytest test/ --benchmark-only

# Run a specific test file
uv run pytest test/test_slab.py -v

# Check Rust code without building
cargo check

# Lint Rust code
cargo clippy

# Build release-mode extension (for performance measurement)
uv run maturin develop --uv --release
```

---

## Physics Domain

### Units and Conventions
- All **lengths** (layer thickness `d`) are in **micrometers (µm)**.
- The input parameter named `omega` in the API is actually **k₀ = ω/c = 2π/λ** in
  natural units where c = 1, with units of **rad/µm**. Example: `omega = 2π/1.55` for
  1550 nm light.
- **neff** (effective refractive index) is dimensionless. For guided modes it is real;
  for lossy or leaky modes it is complex.
- The parallel wavevector: `k_parallel = neff * omega`.
- The transverse wavevector in layer i: `kz = kz_physical(k0, n_i, k_parallel)`.
  See the branch-cut section below.

### Layer Stack Convention
- Layers are ordered **left to right**: `layers[0]` is the left cladding,
  `layers[-1]` is the right cladding.
- The first and last layers act as semi-infinite claddings (their thickness `d` is
  ignored by the solver).
- Mode indices start at 0 (0 = fundamental mode).

### Refractive Index
- `Layer.n` is stored as `Complex<f64>`.
- For lossless media pass a plain `float` from Python — it is promoted to
  `Complex { re: n, im: 0.0 }` automatically (fully backward-compatible).
- For lossy or gain media pass a Python `complex`, e.g. `Layer(1.5 + 0.01j, 0.6)`.
- The CLI TOML format continues to use plain floats for `n` via the `SerdeLayer` shim
  in `bin/remsol.rs`.

### Polarizations
- **TE** (Transverse Electric): `Ey` is the dominant field component.
- **TM** (Transverse Magnetic): `Hy` is the dominant field component.
  Note: for TM, `Ez` is **not** continuous across dielectric interfaces — only `Hy`
  and `Dz = n²·Ez` are continuous.

### Boundary Conditions
- **SemiInfinite** (default): the outermost layers are semi-infinite claddings; the
  field decays evanescently outward.
- **PEC** (Perfect Electric Conductor): the tangential electric field is forced to zero
  at the boundary wall. Can be applied to the left or right side (not both).

### Backends
- **Transfer Matrix Method (TMM)** — `BackEnd::Transfer` — supports both `neff()` and
  `field()` / `field_complex()`. Default backend.
- **Scattering Matrix Method (SMM)** — `BackEnd::Scattering` — supports `neff()` only;
  field calculation is not implemented.
- The complex-plane mode search (`complex_neff` / `all_complex_neff`) **always uses the
  SMM**, regardless of the `backend` setting, because the SMM is unconditionally
  numerically stable for complex `k` (it never forms products of exponentially growing
  and decaying terms in the same matrix element).
- Field reconstruction (`field()` and `field_complex()`) always uses the TMM backend.

---

## Branch-Cut Convention for kz

All transverse wavevectors are computed via `kz_physical(k0, n, k)` in
`transfer_matrix.rs`, which enforces **Im(kz) ≥ 0** (and Re(kz) ≥ 0 when Im(kz) = 0)
everywhere in the complex plane.

This is the correct physical sheet:
- Propagating layers: `Im(kz) = 0`, `Re(kz) > 0` — energy travels in +x.
- Evanescent layers: `Re(kz) = 0`, `Im(kz) > 0` — field decays in +x.

The only problematic point is when the argument of sqrt lands exactly on the negative
real axis (the branch cut), where the principal sqrt returns `Im = 0, Re < 0`. This
case is detected and the sign flipped explicitly.

**Never replace `kz_physical` with a bare `.sqrt()` call** — the bare call will give
wrong results for complex arguments near the branch cut.

---

## Mode Finding: Two Solvers

### 1. Real-axis solver — `neff` / `all_neff`

Scans `Re(neff)` over `[Re(n_min), Re(n_max)]` and detects peaks in `log|1/t22|` (TMM)
or `log|det(S)|` (SMM). Fast, reliable, and suitable for lossless guided modes.
Returns real `float` values.

### 2. Complex-plane solver — `complex_neff` / `all_complex_neff`

Finds modes in a rectangle of the complex `neff` plane using a three-stage algorithm.
Returns complex `(Re(neff), Im(neff))` tuples. Handles:
- **Lossy guided modes**: `Im(n) ≠ 0` in one or more layers → `Im(neff) ≠ 0`.
- **Leaky modes**: `Re(neff) < Re(n_clad)` → field radiates into the cladding.

**Default search rectangle:**
- `re_range = (Re(n_min), Re(n_max))` — same bounds as the real-axis solver.
- `im_range = (-w, +w)` where `w = max(max_j |Im(n_j)|, 1e-3)`.

**Algorithm:**

1. **Argument-principle winding number** (`winding_number`): evaluates `f(k) = 1/det(S(k))`
   on the four sides of the rectangle and accumulates `Σ Δarg(f)`. Dividing by `2π`
   gives the number of enclosed zeros (modes). Using `1/det(S)` rather than `det(S)` is
   essential: `det(S)` is enormous near a mode and tiny elsewhere, making the argument
   change easy to miss with a coarse contour; `1/det(S)` inverts this. Contour sample
   counts are scaled by aspect ratio so elongated rectangles are never under-sampled.

2. **Recursive bisection** (`find_zeros_in_rectangle`): if winding number > 1, bisect
   along the longer side and recurse. If winding number = 1, send to polisher.
   Maximum recursion depth: 20.

3. **Muller root polisher** (`muller_polish`): fits a quadratic through three complex
   evaluations of `f` and steps to its nearest root. Convergence order ≈ 1.84.
   No derivative required.

**Practical guidance for the search rectangle:**
- For lossless structures, use a square imaginary window (e.g. `im_range=(-0.05, 0.05)`)
  rather than a very thin one. A thin window (e.g. `±1e-4`) produces an extremely
  elongated rectangle where the top/bottom contour sides pass very close to the real-axis
  poles, making the winding number unreliable. Use one moderately-sized square box per
  mode, or let the defaults apply.
- For leaky modes, extend `re_range` below `Re(n_clad)` explicitly, e.g.
  `re_range=(0.0, n_max)`.
- For lossy structures, `im_range` is set automatically from the material loss.

---

## Python API Surface

Exposed classes (registered in `lib.rs` pymodule, typed in `remsol.pyi`):

| Rust type       | Python name     | Purpose                                           |
|-----------------|-----------------|---------------------------------------------------|
| `Polarization`  | `Polarization`  | Enum: `.TE`, `.TM`                                |
| `BackEnd`       | `BackEnd`       | Enum: `.Transfer`, `.Scattering`                  |
| `BoundaryCondition` | `BoundaryCondition` | Enum: `.SemiInfinite`, `.PEC`           |
| `Normalization` | `Normalization` | Enum: `.MaxField` (default), `.Power`             |
| `Layer`         | `Layer`         | `Layer(n: float | complex, d: float)`             |
| `PEC`           | `PEC`           | Boundary marker; place first or last in layer list|
| `MultiLayer`    | `MultiLayer`    | `MultiLayer(layers: list[Layer | PEC])`           |
| `IndexData`     | `IndexData`     | Returned by `MultiLayer.index()`: x and Re(n)     |
| `FieldData`     | `FieldData`     | Returned by `MultiLayer.field()`: x, Ex…Hz arrays |

Key `MultiLayer` methods:

| Method | Returns | Notes |
|--------|---------|-------|
| `neff(omega, polarization, mode)` | `float \| None` | Real-axis solver |
| `all_neff(omega, polarization)` | `list[float]` | Real-axis solver |
| `complex_neff(omega, polarization, mode, re_range, im_range)` | `tuple[float,float] \| None` | Complex-plane solver |
| `all_complex_neff(omega, polarization, re_range, im_range)` | `list[tuple[float,float]]` | Complex-plane solver |
| `field(omega, polarization, mode)` | `FieldData` | TMM only; real neff from real-axis solver |
| `field_complex(omega, polarization, neff_re, neff_im)` | `FieldData` | TMM; accepts explicit complex neff |
| `index()` | `IndexData` | Real part of n |
| `set_left_boundary(bc)` | `None` | Alternative to PEC marker |
| `set_right_boundary(bc)` | `None` | Alternative to PEC marker |
| `set_normalization(norm)` | `None` | Switch field normalization convention |

---

## Field Normalization

Two normalization conventions are available, controlled by `MultiLayer.set_normalization()`:

### `Normalization.MaxField` (default)
Divides all field components by the maximum total electric field amplitude:
`max(sqrt(|Ex|² + |Ey|² + |Ez|²)) = 1`.  Works for **all** modes: lossless guided,
lossy guided, and leaky.  This is the default.

### `Normalization.Power`
Divides all field components by `sqrt(|∫ (Ex·Hy* − Ey·Hx*) dx|)` so that the
absolute value of the integrated z-component of the Poynting vector equals 1.
Only physically meaningful for **lossless guided modes** (real neff, finite power).

**Warning behaviour**: if `Normalization.Power` is requested via `field_complex()` and
`Im(neff) ≠ 0`, the library emits a `WARNING`-level log message through Python's
standard `logging` module and falls back to `MaxField` normalization automatically.
Users can observe or suppress this warning using the standard Python logging API:
```python
import logging
logging.basicConfig(level=logging.WARNING)  # show all warnings
logging.getLogger("remsol").setLevel(logging.ERROR)  # suppress remsol warnings
```

---

## Logging

REMSOL uses the `log` crate (Rust) bridged to Python's `logging` module via `pyo3-log`.
This means all Rust-side log messages (warnings, debug info) are routed to the Python
`logging` system automatically when the extension is imported.

- The logger name mirrors the Rust module path, with `::` replaced by `.`
  (e.g. `remsol::multilayer` → `remsol.multilayer`).
- Standard Python logging configuration applies:
  ```python
  import logging
  logging.basicConfig(level=logging.DEBUG)   # see all messages
  logging.getLogger("remsol").setLevel(logging.WARNING)  # only warnings+
  ```
- Do **not** use `eprintln!` or `println!` for diagnostic output in Rust — use the
  `log` macros (`warn!`, `info!`, `debug!`, `trace!`) so that Python users can control
  verbosity.

---

## Code Style and Conventions

### Rust
- All public structs, enums, and functions must have `///` doc comments.
- PyO3 Python-visible methods go in a dedicated `#[pymethods] impl` block, separate from
  the pure-Rust `impl` block.
- Internal Rust-only helpers are `fn` (private); only promote to `pub fn` when needed
  across modules.
- Avoid `clippy` warnings — CI runs `cargo test --lib`.
- Use `num_complex::Complex<f64>` for all complex arithmetic.
- All transverse wavevectors must go through `kz_physical` — never use bare `.sqrt()`.
- The complex-plane solver always uses the SMM backend. Do not add a TMM path for it.
- Use `log::warn!` / `log::info!` / `log::debug!` for all diagnostic output — never
  `eprintln!` or `println!` in library code.

### Python / Type Stubs
- **Always keep `remsol.pyi` in sync** whenever the Python-visible API changes in
  `lib.rs`. This file is the public contract for Python users and IDEs.
- Tests use `pytest.approx` for floating-point assertions.
- Test fixtures (MultiLayer instances) are defined at module level in each test file and
  reused across test functions.

### Versioning
- Version lives in `Cargo.toml` (`package.version`) only; `pyproject.toml` pulls it
  dynamically via Maturin.
- `python-semantic-release` bumps the version and generates `CHANGELOG.md` from
  conventional commits.
- Use conventional commit messages: `feat:`, `fix:`, `refactor:`, `test:`, `build:`,
  `docs:`.
- Version bump rules (Semantic Versioning):
  - `feat:` → **minor** bump (e.g. 0.2.4 → 0.3.0)
  - `fix:` or `perf:` → **patch** bump (e.g. 0.2.4 → 0.2.5)
  - `BREAKING CHANGE:` footer or `feat!:`/`fix!:` → **major** bump
  - `build:`, `chore:`, `ci:`, `docs:`, `style:`, `refactor:`, `test:` → no bump
- Releases are made from the `main` branch only. Develop on feature branches, merge to
  `main` when ready to release.

---

## Secrets and API Access

Sensitive credentials (e.g. API tokens) are stored in a **`.secrets`** file at the
repository root. This file is listed in `.gitignore` and must never be committed.

Format:
```
GITHUB_TOKEN=github_pat_...
```

To access the GitHub API (e.g. to fetch Actions logs), read the token from `.secrets`:

```bash
GITHUB_TOKEN=$(grep GITHUB_TOKEN .secrets | cut -d= -f2)
curl -s -H "Authorization: Bearer $GITHUB_TOKEN" \
     -H "Accept: application/vnd.github+json" \
     "https://api.github.com/repos/mpasson/REMSOL/actions/runs?per_page=5"
```

The token has **read-only** access to the `mpasson/REMSOL` repository:
- **Actions** — read workflow runs and logs
- **Contents** — read repository files
- **Metadata** — read repository metadata

---

## Release Workflow

Releases are managed locally with `python-semantic-release` and then pushed to trigger
CI (which builds wheels and publishes to PyPI/crates.io).

```bash
# 1. Ensure you are on main and up to date
git checkout main
git pull

# 2. Ensure the working tree is clean
git status

# 3. Dry-run to preview the next version and changelog
uv run semantic-release --noop version

# 4. Create the release commit, tag, and update CHANGELOG.md — NO push
uv run semantic-release version --no-push --no-vcs-release

# 5. Review the commit and tag, then push when ready
git log --oneline -3
git push && git push --tags
```

> **Important:** Always ensure `git status` shows a **clean working tree** before step 4.
> Commit or stash everything first.
>
> Step 4 will: bump the version in `Cargo.toml`, update `CHANGELOG.md`, create a release
> commit and an annotated git tag (e.g. `v0.3.0`).
>
> Pushing the tag triggers the `publish_pypi.yml` and `publish_crate.yml` CI workflows.

---

## Development Workflow for New Features

1. Create a feature branch from `main`.
2. Implement the feature in Rust (`src/`).
3. If the feature should be Python-visible:
   a. Add `#[pymethods]` in the relevant `impl` block.
   b. Register new classes in `lib.rs` pymodule if needed.
   c. Update `remsol.pyi` with the new type stubs and docstrings.
4. Run `uv run maturin develop --uv` to rebuild the extension.
5. Add or update Rust unit tests in `mod tests` inside the relevant `.rs` file.
6. Add or update Python integration tests in `test/`.
7. Run `cargo test --lib` and `uv run pytest test/` — both must pass.
8. Commit with conventional commit messages (one logical unit per commit).

---

## Known Limitations and Out-of-Scope Items

- Only **1D planar** structures; no 2D or 3D geometries.
- The **SMM backend does not support field calculation** (`field()` and `field_complex()`
  always use the TMM backend regardless of `MultiLayer.backend`).
- The **complex-plane solver does not support PEC boundary conditions** — it always uses
  the SMM with semi-infinite boundary conditions.
- `field_complex()` does not support PEC boundary conditions for the same reason: the
  complex neff search (which provides the input neff) requires semi-infinite boundaries.
- The `index()` method returns only `Re(n)` for plotting; imaginary parts of the index
  profile are not currently visualised.
- **Power normalization** (`Normalization.Power`) is only physically meaningful for
  lossless guided modes. For complex neff it falls back to `MaxField` with a warning.